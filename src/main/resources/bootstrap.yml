spring:
  application:
    name: monedero
  config:
    import: "optional:configserver:https://ms-configserver-team.azurewebsites.net"
  cloud:
    config:
      enabled: true
      uri: http://localhost:8091
      profile: dev
